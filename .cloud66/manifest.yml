disaster_recovery:
  rails:
    servers:
    - server:
        unique_name: DR_app
        vendor: aws
        region: us-east-1
        size: m3.medium
        root_disk_size: 100
        root_disk_type: ssd
        availability_zone: us-east-1c
