apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argo-postgres
  namespace: argo
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secret-manager
    kind: ClusterSecretStore
  target:
    name: argo-postgres
    creationPolicy: Owner
  data:
    - secretKey: host
      remoteRef:
        key: credreg-argo-workflows
        property: host
    - secretKey: port
      remoteRef:
        key: credreg-argo-workflows
        property: port
    - secretKey: database
      remoteRef:
        key: credreg-argo-workflows
        property: database
    - secretKey: username
      remoteRef:
        key: credreg-argo-workflows
        property: username
    - secretKey: password
      remoteRef:
        key: credreg-argo-workflows
        property: password
    - secretKey: sslmode
      remoteRef:
        key: credreg-argo-workflows
        property: sslmode
