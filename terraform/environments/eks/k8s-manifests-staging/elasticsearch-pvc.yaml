apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: credreg-staging
  name: elasticsearch-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: gp3
