{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "CredentialRegistry Credential metadata",
  "type": "object",

  "definitions": {
    "credreg_credential": {
      "properties": {
        "@type": { "enum": ["cti:Credential"] }

      },

      "required": [ "@type" ]
    }
  },

  "allOf": [
    { "$ref": "<%= ref :json_ld %>" },
    { "$ref": "#/definitions/credreg_credential" }
  ]
}
