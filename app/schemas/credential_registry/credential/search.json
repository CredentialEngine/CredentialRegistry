{
  "description": "Search config for CredentialRegistry/Credential",

  "fts": [
    {"prop": "schema:name", "weight": 5},
    {"prop": "schema:description", "weight": 1}
  ],
  "properties": {
  }
}
